import {Modal} from "./components/Modal.tsx";

function App() {
    return (
        <main className="min-h-screen flex items-center justify-center bg-mainBG text-white flex flex-col items-center justify-center">
            <Modal/>
        </main>
    );
}

export default App;
